name: Docker Build & Push to GHCR

on:
  push:
    branches: [ "devops/github-action-pipeline" ]

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and Push to GHCR
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/tech-from-palestine-devops/java-mock-app:latest

      # - name: Build and tag image
      #   run: |
      #     COMMIT_SHA=$(echo $GITHUB_SHA | cut -c1-7)
      #     docker build -t ghcr.io/${{ github.repository_owner }}/${{ github.repository }}:$COMMIT_SHA .    

      # - name: Push image to GHCR
      #   run: docker push ghcr.io/${{ github.repository_owner }}/${{ github.repository }}:$COMMIT_SHA